import {
    customerPath,
    loginPath,
    serviceProvidedPath,
    SignUpPath,
} from "@/main/docs/paths";

export const paths = {
    "/login": loginPath,
    "/signup": SignUpPath,
    "/customers": customerPath,
    "/services": serviceProvidedPath,
};
