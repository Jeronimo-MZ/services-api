import {
    badRequest,
    notFound,
    serverError,
    unauthorized,
} from "@/main/docs/components";

export const components = {
    badRequest,
    serverError,
    unauthorized,
    notFound,
};
